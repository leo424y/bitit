<div>
  <div id="masonry-container" class="transitions-enabled centered clearfix masonry">
    <% @groups.each do |group| %>
      <div class="box col2" align="center" >
        <%= link_to(image_tag("https://tools.wmflabs.org/idsgen/#{group.title}.png?字體=宋體粗體", size: "100", alt: group.title, class: "img-responsive"), group_path(group))%>
        <p>
          <!-- <%= render_group_description(group) %> -->
          <!-- <%= link_to(render_group_title(group), group_path(group)) %> -->
          <!-- <%= group.posts.size %> -->
          <!-- <%= group.updated_at.to_s(:short)  %> -->
        </p>
      </div>
    <% end %>
  </div>
  <div align="center">
    <%= render "common/mainfunction" %>
    <%= react_component "WorldOnline" %>
    <%= paginate @groups, :theme => 'twitter-bootstrap-3' %>
  </div>
</div>
